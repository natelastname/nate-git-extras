
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "nate_git_tree"
version = "0.1.0"
description = "A python module"
readme = "README.md"
authors = [
  { name = "Nathan Nichols", email = "communications@resultsmotivated.com" },
]
requires-python = ">=3.12,<3.14.1"

dependencies = [
  "loguru>=0.7.3,<0.8.0",
]

[project.scripts]
nate_git_cp = "nate_git_tree.nate_git_cp:main"
nate_git_ls = "nate_git_tree.nate_git_ls:main"

[project.optional-dependencies]
dev = [
  "pytest>=8,<9",
  "basedpyright>=1.31.5,<2.0.0",
  "ruff>=0.13.2,<0.14.0",
  "mkdocs<2.0",
  "mkdocs-material>=9.7.2,<10.0.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
filterwarnings = [
    "error",
    "ignore::DeprecationWarning",
    "ignore:.*unclosed.*:ResourceWarning",
]

[tool.pyright]
typeCheckingMode = "off"
reportMissingParameterType = "none"
reportMissingTypeArgument = "none"
reportUnknownParameterType = "none"
reportUnknownMemberType = "none"
reportUnknownVariableType = "none"
reportUntypedFunctionDecorator = "none"

[tool.ruff]
src = ["src", "tests"]
line-length = 88

[tool.ruff.lint]
select = [
  "F",  # real errors
  "I",  # import sorting
]
fixable = ["ALL"]

[tool.ruff.lint.per-file-ignores]
"src/**/__init__.py" = ["F401"]
"tests/*" = ["I001"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
